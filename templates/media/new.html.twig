{% extends 'base.html.twig' %}

{% block title %}New Media{% endblock %}

{% block body %}
    <div class="content">
        <div class="row header">
            <div class="col-3 offset-lg-1 col-lg-2">
                {{ include('common/header.html.twig', {'class': 'bi-link-45deg header-icon'}) }}
            </div>
            <div class="col-6 align-self-center text-center">
                <h1>Create new media for "{{song.name}}"</h1>
            </div>
        </div>

        <div class="d-flex justify-content-center">
            <hr class="content-hr-primary">
        </div>

        <div class="custom-form mt-5">
            {{ include('media/_form.html.twig', {'button_label': 'Add Media'}) }}
        </div>

        <div class="mt-3 d-flex align-items-center flex-column">
            <a href="{{ path('app_artist_index') }}" class="back-button mt-1">
                <i class="bi bi-arrow-left-circle"></i> back to artist list
            </a>
        </div>

    </div>


{% endblock %}