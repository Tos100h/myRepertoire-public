{% extends 'base.html.twig' %}

{% block title %}Edit Media{% endblock %}

{% block body %}
    {# <h1>Edit Media</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="btn btn-primary mt-1">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('app_song_index') }}">back to list</a> #}

    <div class="content">
        <div class="row header">
            <div class="col-3 offset-lg-1 col-lg-2">
                {{ include('common/header.html.twig', {'class': 'bi-link-45deg header-icon'}) }}
            </div>
            <div class="col-6 align-self-center text-center">
                <h1>Edit "{{ media.name }}" for <strong>{{ media.song.name }}</strong></h1>
            </div>
            <div class="col-3 offset-lg-1 col-lg-2">
                {{ include('media/_delete_form.html.twig') }} 
            </div>
        </div>

        <div class="d-flex justify-content-center">
            <hr class="content-hr-primary">
        </div>
     
        <div class="custom-form mt-5">
            {{ include('media/_form.html.twig') }}
        </div>

        <div class="d-flex justify-content-center">
            <div class="mt-5">
                <a href="{{ path('app_song_show', {'id': media.song.id }) }}" class="back-button"><i class="bi bi-arrow-left-circle"></i> back to song</a>
            </div>
        </div>
    </div>
{% endblock %}